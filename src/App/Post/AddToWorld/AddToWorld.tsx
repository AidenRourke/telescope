import React, { FC } from 'react';
import styled from 'styled-components';

const AddToWorldContainer = styled.div``;

const AddToWorld: FC = () => {
  return (
    <AddToWorldContainer>
      <h2>ADD TO WORLD:</h2>
    </AddToWorldContainer>
  );
};

export { AddToWorld };
